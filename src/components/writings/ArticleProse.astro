<div class="article-prose">
  <slot />
</div>

<script>
  import "../../scripts/article-code-blocks";
</script>

<style>
  .article-prose {
    min-width: 0;
    color: var(--color-ink-soft);
    font-family: var(--font-sans);
    font-size: var(--font-size-article);
    font-weight: var(--font-weight-regular);
    line-height: var(--line-height-article);
    overflow-wrap: anywhere;
  }

  .article-prose :global(h2),
  .article-prose :global(h3),
  .article-prose :global(h4),
  .article-prose :global(h5),
  .article-prose :global(h6) {
    scroll-margin-top: 1.5rem;
    color: var(--color-ink-strong);
    font-weight: var(--font-weight-medium);
    text-wrap: balance;
  }

  .article-prose :global(h2) {
    margin: 1.75rem 0 0.65rem;
    font-size: 1rem;
    line-height: 1.3;
    letter-spacing: -0.015em;
  }

  .article-prose :global(h3) {
    margin: 1.4rem 0 0.5rem;
    font-size: 0.9375rem;
    line-height: 1.35;
    letter-spacing: -0.01em;
  }

  .article-prose :global(h4),
  .article-prose :global(h5),
  .article-prose :global(h6) {
    margin: 1.15rem 0 0.45rem;
    font-size: 0.875rem;
    line-height: 1.35;
  }

  .article-prose :global(p) {
    margin: 0 0 0.85rem;
  }

  .article-prose :global(strong) {
    color: var(--color-ink-strong);
    font-weight: var(--font-weight-medium);
  }

  .article-prose :global(a) {
    color: var(--color-ink-strong);
    text-decoration-thickness: 0.08em;
    text-underline-offset: 0.18em;
  }

  .article-prose :global(a:hover) {
    background: var(--color-accent);
    text-decoration: none;
  }

  .article-prose :global(a:focus-visible) {
    outline: var(--focus-ring);
    outline-offset: var(--focus-offset);
  }

  .article-prose :global(ul),
  .article-prose :global(ol) {
    margin: 0 0 1.25rem;
    padding-inline-start: 1.5rem;
  }

  .article-prose :global(li + li) {
    margin-top: 0.4rem;
  }

  .article-prose :global(li > ul),
  .article-prose :global(li > ol) {
    margin-top: 0.4rem;
    margin-bottom: 0;
  }

  .article-prose :global(.contains-task-list) {
    padding-inline-start: 0;
    list-style: none;
  }

  .article-prose :global(.task-list-item) {
    display: flex;
    align-items: baseline;
    gap: 0.6rem;
  }

  .article-prose :global(input[type="checkbox"]) {
    width: 1rem;
    height: 1rem;
    margin: 0;
    accent-color: var(--color-ink-strong);
  }

  .article-prose :global(blockquote) {
    margin: 1.75rem 0;
    padding: 0.25rem 0 0.25rem 1.25rem;
    border-inline-start: 0.2rem solid var(--color-accent);
    color: var(--color-ink);
  }

  .article-prose :global(blockquote > :last-child) {
    margin-bottom: 0;
  }

  .article-prose :global(code) {
    padding: 0.12rem 0.3rem;
    background: var(--color-surface-soft);
    color: var(--color-ink-strong);
    font-family:
      "SFMono-Regular", Consolas, "Liberation Mono", ui-monospace, monospace;
    font-size: 0.92em;
  }

  .article-prose :global(pre) {
    position: relative;
    max-width: 100%;
    margin: 1.5rem 0;
    overflow: hidden;
    border: 1px solid var(--cb-bar-border);
    border-radius: 8px;
    padding: 0;
    background: var(--color-code-bg) !important;
    box-shadow: var(--cb-shadow-resting);
    tab-size: 4;
    transition: box-shadow 0.3s cubic-bezier(0.4, 0, 0.2, 1);
  }

  .article-prose :global(pre:hover) {
    box-shadow: var(--cb-shadow-hover);
  }

  .article-prose :global(pre)::before {
    content: "";
    position: absolute;
    top: 0;
    right: 0;
    left: 0;
    z-index: 10;
    height: var(--cb-mac-bar-height);
    border-radius: 8px 8px 0 0;
    border-bottom: 1px solid var(--cb-bar-border);
    background: var(--cb-bar-bg);
    pointer-events: none;
    user-select: none;
  }

  .article-prose :global(pre)::after {
    content: "";
    position: absolute;
    top: calc((var(--cb-mac-bar-height) - var(--cb-mac-dot-size)) / 2);
    left: 16px;
    z-index: 11;
    width: var(--cb-mac-dot-size);
    height: var(--cb-mac-dot-size);
    border-radius: 50%;
    background: var(--cb-dot-red);
    box-shadow:
      20px 0 0 var(--cb-dot-yellow),
      40px 0 0 var(--cb-dot-green);
    pointer-events: none;
    user-select: none;
  }

  .article-prose :global(pre code) {
    display: block;
    max-width: 100%;
    max-height: var(--cb-max-height);
    overflow: auto;
    padding: calc(var(--cb-mac-bar-height) + var(--cb-code-padding))
      var(--cb-code-padding) var(--cb-code-padding) 0;
    background: transparent;
    color: #24292f;
    font-family:
      "SFMono-Regular", Consolas, "Liberation Mono", ui-monospace, monospace;
    font-size: var(--font-size-code);
    line-height: 1.6;
    white-space: normal;
    word-wrap: normal;
    counter-reset: line;
    scrollbar-width: thin;
    scrollbar-color: var(--cb-scrollbar-thumb) var(--cb-scrollbar-track);
    -webkit-overflow-scrolling: touch;
  }

  .article-prose :global(pre .line) {
    display: block;
    padding-inline-start: calc(var(--cb-gutter-width) + var(--cb-gutter-gap));
    white-space: pre;
  }

  .article-prose :global(pre .line)::before {
    content: counter(line);
    counter-increment: line;
    position: sticky;
    left: 0;
    display: inline-block;
    width: var(--cb-gutter-width);
    margin-inline-start: calc(-1 * (var(--cb-gutter-width) + var(--cb-gutter-gap)));
    margin-inline-end: var(--cb-gutter-gap);
    background: var(--color-code-bg);
    color: var(--cb-gutter-color);
    font-variant-numeric: tabular-nums;
    text-align: end;
    user-select: none;
    pointer-events: none;
  }

  .article-prose :global(pre code)::-webkit-scrollbar {
    width: 8px;
    height: 8px;
  }

  .article-prose :global(pre code)::-webkit-scrollbar-thumb {
    border: 2px solid var(--cb-scrollbar-track);
    border-radius: 4px;
    background: var(--cb-scrollbar-thumb);
  }

  .article-prose :global(pre code)::-webkit-scrollbar-thumb:hover {
    background: var(--color-label-muted);
  }

  .article-prose :global(pre code)::-webkit-scrollbar-track,
  .article-prose :global(pre code)::-webkit-scrollbar-corner {
    background: var(--cb-scrollbar-track);
  }

  .article-prose :global(.copy-code-button) {
    position: absolute;
    top: 0;
    right: 10px;
    z-index: 12;
    display: inline-flex;
    min-width: 3.25rem;
    height: var(--cb-mac-bar-height);
    align-items: center;
    justify-content: center;
    border: 0;
    border-radius: 0;
    padding: 0 8px;
    background: transparent;
    color: var(--color-label-muted);
    font-family: var(--font-sans);
    font-size: var(--font-size-body);
    font-weight: var(--font-weight-medium);
    line-height: var(--line-height-body);
    cursor: pointer;
    transition:
      background-color 0.2s cubic-bezier(0.4, 0, 0.2, 1),
      color 0.2s cubic-bezier(0.4, 0, 0.2, 1);
  }

  .article-prose :global(.copy-code-button:hover) {
    background: var(--cb-button-bg-hover);
    color: var(--color-ink-strong);
  }

  .article-prose :global(.copy-code-button:active) {
    background: var(--cb-button-bg-active);
  }

  .article-prose :global(.copy-code-button:focus-visible) {
    outline: var(--focus-ring);
    outline-offset: var(--focus-offset);
  }

  .article-prose :global(.copy-code-button.is-copied),
  .article-prose :global(.copy-code-button.is-failed) {
    top: calc((var(--cb-mac-bar-height) - 1.2em - 4px) / 2);
    height: auto;
    min-width: 0;
    padding: 2px 5px;
    background: var(--color-ink-strong);
    color: var(--color-pill-ink);
  }

  .article-prose :global(.copy-code-button.is-failed) {
    background: var(--color-surface-soft);
    color: var(--color-ink-strong);
  }

  .article-prose :global(.copy-code-toast) {
    position: absolute;
    top: calc(var(--cb-mac-bar-height) + 0.5rem);
    right: 10px;
    z-index: 13;
    display: inline-flex;
    align-items: center;
    padding: 2px 5px;
    background: var(--color-ink-strong);
    color: var(--color-pill-ink);
    font-family: var(--font-sans);
    font-size: var(--font-size-body);
    font-weight: var(--font-weight-medium);
    line-height: var(--line-height-body);
    pointer-events: none;
  }

  .article-prose :global(.copy-code-toast.is-failed) {
    background: var(--color-surface-soft);
    color: var(--color-ink-strong);
  }

  .article-prose :global(.copy-code-toast[hidden]) {
    display: none;
  }

  .article-prose :global(table) {
    display: block;
    width: 100%;
    max-width: 100%;
    margin: 1.5rem 0;
    overflow-x: auto;
    border-collapse: collapse;
    font-size: 0.92em;
    -webkit-overflow-scrolling: touch;
  }

  .article-prose :global(th),
  .article-prose :global(td) {
    min-width: 8rem;
    border-bottom: 1px solid var(--color-rule);
    padding: 0.65rem 0.75rem;
    text-align: start;
    vertical-align: top;
  }

  .article-prose :global(th) {
    color: var(--color-ink-strong);
    font-weight: var(--font-weight-medium);
  }

  .article-prose :global(img),
  .article-prose :global(picture),
  .article-prose :global(video),
  .article-prose :global(svg) {
    display: block;
    max-width: 100%;
    height: auto;
  }

  .article-prose :global(img),
  .article-prose :global(picture),
  .article-prose :global(video),
  .article-prose :global(figure) {
    margin-block: 1.75rem;
  }

  .article-prose :global(figure) {
    margin-inline: 0;
  }

  .article-prose :global(figure img),
  .article-prose :global(figure picture),
  .article-prose :global(figure video) {
    margin-block: 0;
  }

  .article-prose :global(figcaption) {
    margin-top: 0.6rem;
    color: var(--color-label-muted);
    font-size: var(--font-size-caption);
    line-height: 1.45;
  }

  .article-prose :global(hr) {
    height: 1px;
    margin: 2.5rem 0;
    border: 0;
    background: var(--color-rule);
  }

  .article-prose :global(kbd) {
    border: 1px solid var(--color-rule-strong);
    padding: 0.1rem 0.35rem;
    background: var(--color-surface-soft);
    color: var(--color-ink-strong);
    font-family:
      "SFMono-Regular", Consolas, "Liberation Mono", ui-monospace, monospace;
    font-size: 0.82em;
  }

  .article-prose :global(section[data-footnotes]) {
    margin-top: 3rem;
    padding-top: 1.5rem;
    border-top: 1px solid var(--color-rule);
    color: var(--color-label-muted);
    font-size: var(--font-size-caption);
  }

  .article-prose > :global(:first-child) {
    margin-top: 0;
  }

  .article-prose > :global(:last-child) {
    margin-bottom: 0;
  }

  @media (prefers-reduced-motion: reduce) {
    .article-prose :global(pre),
    .article-prose :global(.copy-code-button) {
      transition: none;
    }
  }

  @media print {
    .article-prose :global(pre) {
      box-shadow: none;
    }

    .article-prose :global(pre code) {
      max-height: none;
      overflow: visible;
      overflow-x: visible;
    }

    .article-prose :global(pre .line) {
      white-space: pre-wrap;
      overflow-wrap: anywhere;
    }

    .article-prose :global(pre code)::-webkit-scrollbar,
    .article-prose :global(.copy-code-button),
    .article-prose :global(.copy-code-toast) {
      display: none;
    }
  }
</style>
